// src/constants/ui.js

/**
 * UI-related constants
 */
export const UI = {
  MIN_TOUCH_HEIGHT: 44,      // Minimum touch target height (Apple HIG)
  MIN_BUTTON_HEIGHT: 48,     // Minimum button height for emphasis
  SCROLL_DELAY: 100,         // Delay for scroll operations (ms)
  DEBOUNCE_DELAY: 300        // Debounce delay for input (ms)
};
